1. El Patron Singleton se aplica a la clase abstracta Logger, o a las concretas ConsoleLogger y FileLogger.
	EL singleton es de Logger y en el getInstance inicializa el console logger y/o el FileLogger
2. Hay manera de que evitar usar CalculadoraAPP?